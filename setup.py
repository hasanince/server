from setuptools import setup

setup(
    name = "server.py",
    version = "0.1",
    py_modules = ['server'],
    url = "https://github.com/hasanince/server/",
    author = "hasanince",
    install_requires = ["pyngrok"]
)
